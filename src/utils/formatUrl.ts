export const formatUrl = (url : string) => {
  const newUrl = url.slice(8,-1)
  let lastIndexToShow;
  // for(let i = 0; i < url.length; i++){
  //   if()
  // }
  // newUrl.lastIndexOf
  return newUrl
}